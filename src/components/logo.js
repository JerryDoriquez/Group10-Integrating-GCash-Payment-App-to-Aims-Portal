import React from 'react';
import bannerText from '../components/bannerText.png';

const logo = () => (
  <header className="header">
    <img src={bannerText} alt="School Logo" />
  </header>
);

export default logo;
